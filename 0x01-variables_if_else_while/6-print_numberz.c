#include <stdio.h>

/**
 * main - prints all number from 10 to 0,
 * followed by a new line
 *Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 18; n < 28; n++)
		putchar("%d", n);
	putchar(n);
	return (0);
}
