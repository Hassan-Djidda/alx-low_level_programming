#include <stdio.h>

/**
 * main - prints all number from 10 to 0,
 * followed by a new line
 *Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		putchar(48 + n);
		c++;
	}
	
	putchar('\n');
	return (0);
}
